<% if response.success? %>
# <%= request.method %> <%= request.path %>

### Request

```
<%=
  JSON.pretty_generate(request.params.except(:controller, :action, :format))
%>
```

### Response

```
<%=
  begin
    if response.body.length > 500
      "Too big to display."
    else
      JSON.pretty_generate(response.body)
    end
  rescue => e
    e.message
  end
%>
```
<% end %>
