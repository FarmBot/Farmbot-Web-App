.widget-wrapper
  .row
    .small-12.columns
      .row
        .small-12.columns
          .header-wrapper
            %h5 Logs
      .row
        .small-12.columns
          %table
            %thead
              %tr
                %th{width: '15%'}
                  %label TIME
                %th{width: '80%'}
                  %label MESSAGE
                %th{width: '5%'}
                  %label COORDINATES
            %tbody{ng_if: "logs.length > 1"}
              %tr{'ng-repeat' => 'log in logs'}
                %td {{ log.timestamp | date:'MM/dd h:mma' }}
                %td {{ log.data || "-" }}
                %td {{ (log.X || '-') + ', ' + (log.Y || '-') + ', ' + (log.Z || '-') }}
            %tbody{ng_if: "logs.length < 1"}
              %tr
                %td{colspan: '3'}
                  We can't find any logs. Are your FarmBot device credentials correct?
