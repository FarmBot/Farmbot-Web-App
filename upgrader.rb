# Create list of deps
# Iterate over deps
# Install latest dep
# Begin automated checks:
  # run type checker
  # run tests
  # Run build
    #  Failures? "#{dep} failed to upgrade because it broke #{step}"
